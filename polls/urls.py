from django.conf.urls import patterns, include, url

urlpatterns = patterns('polls.views',
    # Examples:
    # url(r'^$', 'wechatapp.views.home', name='home'),
    # url(r'^blog/', include('blog.urls')),
    # url(r'^welcome/(\d+)$', '', name="question"),
    # url(r'^answer/(\d+)$', '', name="question"),
    # url(r'^result/(\d+)$', '', name="question"),
)
